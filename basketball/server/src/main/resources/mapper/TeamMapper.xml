<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace等于mapper接口类的全限定名,这样实现对应 -->
<mapper namespace="server.mapper.TeamMapper">
    <select id="selectteams" resultType="com.basketball.vo.team.SelectTeamsVO">
        select id,
        name,
        contact_phone,
        address,

        established_date
        from team
        <where>
            <if test="name!= null">
                team.name LIKE CONCAT('%',#{name},'%')
            </if>
        </where>
    </select>

    <select id="selectTeamById" resultType="com.basketball.vo.team.SelectTeamDetailVO">
        SELECT
        id,
        name,

        established_date AS establishedDate,
        contact_phone AS contactPhone,
        address
        FROM team
        WHERE id = #{teamId}
    </select>
    <select id="selectPlayersByTeamId" resultType="com.basketball.vo.player.PlayerVO">
        SELECT
        p.id,
        p.photo,
        CONCAT(p.last_name, p.first_name) AS name,
        p.date_of_birth AS dateOfBirth,
        pos.name AS position,
        p.shirt_number AS shirtNumber
        FROM player p
        LEFT JOIN position pos ON p.position_id = pos.id
        WHERE p.team_id = #{teamId}
        <if test="playerName != null and playerName != ''">
            AND CONCAT(p.last_name, p.first_name) LIKE CONCAT('%', #{playerName}, '%')
        </if>
        ORDER BY p.shirt_number ASC
    </select>
    <select id="selectByName" resultType="com.basketball.entity.Team">
        SELECT
        id,
        name,
        contact_phone AS contactPhone,
        address,
        logo,
        established_date AS establishedDate
        FROM team
        WHERE name = #{name}
        LIMIT 1
    </select>
    <update id="updateTeam">
        UPDATE team
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="contactPhone != null">
                contact_phone = #{contactPhone},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            <if test="logoBytes != null">
                logo = #{logoBytes, jdbcType=BLOB},
            </if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>